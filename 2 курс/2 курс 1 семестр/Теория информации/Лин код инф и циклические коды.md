# **1. Помехоустойчивые коды: понятие, методики применения**

## Проблема передачи информации в реальных каналах
- Теоретически требуемая вероятность ошибки при передаче — не хуже **10⁻⁶**.
- На практике в реальных каналах она составляет **10⁻² – 10⁻⁵**.
- Решение — применение **помехоустойчивых кодов**, способных **обнаруживать и исправлять ошибки**.
## Что такое помехоустойчивый код?
- Код, построенный так, что **разрешённые кодовые слова** (комбинации) отличаются друг от друга **более чем на один символ**.
- Все остальные комбинации — **запрещённые** (ошибочные).
- При приёме если принята запрещённая комбинация — это сигнал об ошибке; если она близка к разрешённой — можно попытаться её исправить.
## Основные параметры помехоустойчивого кода

| Параметр | Обозначение | Описание |
|----------|-------------|----------|
| Длина кодовой комбинации | n | Общее число символов в кодовом слове |
| Длина информационной последовательности | k | Число информационных символов |
| Длина проверочной последовательности | r = n - k | Число контрольных (избыточных) символов |
| Кодовое расстояние | d | Расстояние Хэмминга между двумя кодовыми словами |
| Минимальное кодовое расстояние | d_min | Минимум среди всех d между парами кодовых слов |
| Скорость кода | R = k/n | Относительная эффективность передачи |
| Избыточность | ρ = 1 - R | Доля избыточных символов |
| Вероятность обнаружения ошибки | P_d | Вероятность, что ошибка будет обнаружена |
| Вероятность необнаруженной ошибки | P_u | Вероятность, что ошибка останется незамеченной |

> **Связь d_min с корректирующей способностью:**
> - Исправление до **t** ошибок: **d_min ≥ 2t + 1**
> - Обнаружение до **s** ошибок: **d_min ≥ s + 1**
## Границы для кодов
- **Верхняя граница Хэмминга (сферическая граница):**
  \[
  \sum_{i=0}^{t} \binom{n}{i} \leq 2^{n-k}
  \]
  Ограничивает максимальную скорость кода при заданной корректирующей способности.

- **Нижняя граница Плоткина (или Варшамова–Гильберта):**
  Указывает, что при определённых условиях существуют коды с нужными параметрами.

> ⚠️ *Примечание:* "Граница Варшамова–Гильберта" — более корректное название нижней границы, чем "Плотина" или "Интальна".

---
## Подклассы помехоустойчивых кодов

### Сверхточные коды
- Отличаются **методом построения** (с памятью) и областью применения.
- Кодирование зависит от предыдущих символов → **древовидная структура**.
- Чем длиннее кодовая последовательность — тем выше помехоустойчивость, но сложнее реализация.
### Составные коды
- **Каскадные коды**: состоят из двух ступеней — внутреннего и внешнего кода.
  - Внутренний код работает на уровне символов.
  - Внешний код работает на уровне блоков (слов) внутреннего кода.
- **Коды произведения**: строятся в виде матрицы:
  - строки — кодовые слова одного кода,
  - столбцы — кодовые слова другого кода.
### Блоковые vs Древовидные коды
- **Блоковые коды**: без памяти. Каждый блок из `k` информационных символов кодируется независимо в `n` символов.
  - Параметры: `n`, `k`, `R = k/n`
  - Информационная последовательность: от 3 до сотен символов.
  - Скорость: обычно от 1/4 до 1 (но возможны и другие значения).

- **Древовидные (сверточные) коды**: с памятью.
  - На вход подаётся `m` символов, на выходе — `L` символов.
  - Текущий выход зависит от текущего и предыдущих входных символов.
  - Параметры: длина информационной последовательности, скорость, свободное расстояние.
### Линейные коды
- Кодовые слова — линейные комбинации информационных символов (обычно сложение по модулю 2).
- Множество кодовых слов образует **группу** относительно операции сложения → **групповые коды**.
- **Основное свойство группового кода:**
  > Минимальное кодовое расстояние равно минимальному весу ненулевого кодового вектора.
  > 
  > Вес вектора — количество ненулевых компонентов.
  > 
  > Расстояние между двумя векторами = вес их суммы по модулю 2.
### Матричная запись линейных кодов
- Задаются **порождающей матрицей G** размером `k × n`.
- Любое кодовое слово: **c = m·G**, где `m` — информационный вектор.
- **Проверочная матрица H** размером `(n-k) × n` используется для обнаружения ошибок: **H·c^T = 0**.

---
# 2. Линейные коды: параметры и свойства. Особенности блочного кода Хэмминга
## **Линейные коды — основные свойства**
- **Замкнутость относительно сложения**: сумма двух кодовых векторов — тоже кодовый вектор.
- **Образуют группу** по сложению по модулю 2 → **групповые коды**.
- **Минимальное расстояние = минимальному весу ненулевого кодового слова**.
## **Блочный код Хэмминга**
- Пример **линейного блочного кода**.
- Позволяет **исправлять одиночные ошибки** (t = 1).
- Условие: **d_min = 3** → 2t + 1 = 3.
### **Параметры кода Хэмминга**
- Длина кодовой комбинации: **n = 2^m - 1**
- Длина информационной части: **k = 2^m - m - 1**
- Число проверочных символов: **r = m**
- Скорость: **R = k/n = (2^m - m - 1)/(2^m - 1)**

> Пример: при m=3 → n=7, k=4, r=3 → (7,4)-код Хэмминга.
### **Строится с помощью проверочной матрицы H**
- Матрица H размером `m × (2^m - 1)` содержит все ненулевые двоичные столбцы длины `m`.
- Каждый столбец соответствует позиции в кодовом слове.
- Синдром (результат умножения H на принятый вектор) указывает на номер позиции ошибки.

---
# **3. Циклические коды: определение, свойства, обнаружение пакетов ошибок**
## **Определение циклического кода**
- Подкласс линейных кодов.
- Если кодовое слово **c = (c₀, c₁, ..., c_{n-1})** принадлежит коду, то его **циклический сдвиг** **c' = (c_{n-1}, c₀, c₁, ..., c_{n-2})** также принадлежит коду.
- Представляются как многочлены: **c(x) = c₀ + c₁x + ... + c_{n-1}x^{n-1}**
## **Свойства циклических кодов**
- **Порождающий многочлен g(x)** степени `r = n - k` делит `x^n - 1`.
- Любой кодовый многочлен: **c(x) = m(x)·g(x)**, где `m(x)` — информационный многочлен.
- **Проверочный многочлен h(x)**: `x^n - 1 = g(x)·h(x)`
- Обнаружение ошибок через деление принятого многочлена на `g(x)` — если остаток ≠ 0 → ошибка.
## **Обнаружение пакетов ошибок**
- **Пакет ошибок** — последовательность ошибок, идущих подряд (например, в канале с импульсными помехами).
- Циклические коды хорошо подходят для обнаружения пакетов ошибок.
- **Теорема:** Циклический код с порождающим многочленом степени `r` может обнаруживать **все пакеты ошибок длиной ≤ r**.
- Более того, если `g(x)` имеет множитель `(x+1)`, то код обнаруживает **все нечётные ошибки**.

> 📌 **Пример:** CRC-коды — частный случай циклических кодов, используемые для обнаружения ошибок в сетях и хранилищах.

---
# **Итоговая сводка**

| Код | Тип | Память | Коррекция | Применение |
|-----|-----|--------|-----------|------------|
| Блоковый (Хэмминг) | Линейный, блочный | Нет | Одиночные ошибки | Память, простые каналы |
| Сверточный | Линейный, древовидный | Да | Последовательные ошибки | Спутниковая связь, GSM |
| Циклический (CRC) | Линейный, блочный | Нет | Пакеты ошибок | Ethernet, Wi-Fi, файловые системы |
| Каскадный | Композитный | Зависит | Высокая надёжность | Глубокий космос (Voyager), DVB |

# 📊 Избыточность и эффективность кодирования

## 🔍 Основные понятия
*   **Избыточность (Redundancy)** — это "лишняя" информация в коде, которая не несет смысловой нагрузки, но необходима для обеспечения надежности передачи данных.
*   **Цель избыточности**: Повышение помехоустойчивости (устойчивости к ошибкам при передаче) и возможность исправления ошибок.
*   **Примеры использования**: Коды Хэмминга, где добавляются контрольные разряды.

> 💡 *Факт*: При передаче 10 цифр можно использовать 2 кода. Символы одного алфавита (например, цифры) могут быть представлены в другом алфавите (например, двоичном). Символы, количество которых является степенью 2 (например, 8 = 2³), можно передать более компактно. Цифры 5 и 8 требуют разного количества бит.
---
## ⚖️ Расчет избыточности

### a) Частная избыточность (`D0`)

Эта формула показывает, насколько длина кода превышает минимально возможную для передачи данного количества символов.

**Формула:**
$$ D_0 = \frac{K - Y}{K} $$

*   `K` — фактическая длина кода (количество бит).
*   `Y` — минимальная теоретическая длина кода (например, `\log_2 N`, где `N` — количество передаваемых символов).

**Пример:**
Если `K = 3` бита, а минимальная длина Y = $\log_2 5 \approx 2.32$ бита, то:
$$ D_0 = \frac{3 - 2.32}{3} = \frac{0.68}{3} = 0.227 $$
Это означает, что 22.7% кода является избыточным.

> 💬 *Поэтому соглашаемся, что неравенство для цифры 5 будет иметь отношение, как указано выше.*

---
### b) Избыточность для помехоустойчивого кодирования (`Dk`)

Эта формула используется для расчета избыточности в кодах с исправлением ошибок.
**Формулы:**
$$ n_k = n - n_i $$
$$ D_k = \frac{n - n_i}{n_i} $$
*   `n` — общая длина кодового слова (информационные + контрольные биты).
*   `n_i` — количество информационных бит.
*   `n_k` — количество контрольных (избыточных) бит.

> 💡 *Избыточность, используемая для помехоустойчивого кодирования, служит для исправления ошибок. Примеры: коды Хэмминга, где добавляются контрольные разряды.*

---
## 📐 Теоретические пределы кодирования

Эти неравенства задают границы, в которых может находиться средняя длина кода (`\bar{L}`).

### a) Общая формула

Средняя длина кода `\bar{L}` должна быть не меньше, чем отношение энтропии источника `H` к логарифму основания кода `m` (обычно `m=2` для двоичного кода).

**Неравенство:**
$$ \frac{H}{\log m} \leq \bar{L} \leq \frac{H}{\log m} + 1 $$

*   `H` — энтропия источника (мера неопределенности или средней информации на символ).
*   `m` — основание кода (количество символов в алфавите кода, например, 2 для двоичного).
*   $\bar{L}$ — средняя длина кодового слова.

---
### b) Для блочного кодирования

При кодировании блоками по `M` символов, средняя длина на блок $\bar{L}_M$ и на один символ $\bar{L}$ имеют свои ограничения.
**Формулы:**
$$ \frac{MH}{\log m} \leq \bar{L}_M \leq \frac{MH}{\log m} + 1 $$
$$ \frac{H}{\log m} \leq \bar{L} \leq \frac{H}{\log m} + \frac{1}{M} $$

*   `M` — размер блока (количество символов в блоке).
*   $\bar{L}_M$ — средняя длина кода для одного блока из `M` символов.
*   $\bar{L}$ — средняя длина кода на один символ.

> 💬 *Для пас кодирования (побуквенного) в случае применения блокового кодирования, состоящего из нескольких символов $M \to a_1, a_2, ..., a_M$, блочное кодирование выигрывает по сравнению с побуквенным.*

---
## 🎯 Эффективность кодирования

*   **Оптимальные коды** (например, коды Хаффмана) строятся так, чтобы их средняя длина $\bar{L}$ была максимально близка к теоретическому минимуму `H / \log m`.
*   Такие коды обладают практически нулевой избыточностью и являются наиболее эффективными для передачи информации без помех.
*   Блочное кодирование $M > 1$ позволяет достичь большей эффективности, чем побуквенное кодирование $M = 1$, так как уменьшает верхнюю границу для `\bar{L}`.

> 💡 *Наиболее эффективным методом такой избыточности является построение оптимальных кодов, которые обладают практически нулевой избыточностью и имеют минимальную среднюю длину `L`.*

---

## 🧮 Пример расчета минимальной длины кода

Для передачи `N` различных сообщений (или символов) в двоичном коде (`m=2`) минимальная средняя длина кода `L` определяется как:

$$ L \geq \log_2 N $$

**Пример:**
Для передачи 5 символов (`N=5`):
$$ L \geq \log_2 5 \approx 2.32 \text{ бита} $$

Это означает, что в среднем для каждого из 5 символов нужно не менее 2.32 бита. На практике, если использовать фиксированную длину, потребуется 3 бита (так как 2 бита позволяют закодировать только 4 символа: 00, 01, 10, 11).

---

## ✅ Вывод

Избыточность — это необходимое зло для надежной связи. Однако, используя оптимальные методы кодирования, можно свести ее к минимуму, добиваясь максимальной эффективности передачи данных.
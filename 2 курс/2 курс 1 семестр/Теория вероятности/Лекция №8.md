# 🌐 Двумерные случайные величины
## Определение закона распределения
> **Законом распределения** двумерной дискретной случайной величины называется совокупность возможных значений этой величины, т.е. пар чисел $(x_i, y_j)$ и их вероятностей $p(x_i, y_j)$, где $i = 1, 2, ..., n$; $j = 1, 2, ..., m$.

**Табличная форма закона распределения:**

| $Y \backslash X$ | $x_1$ | $x_2$ | ... | $x_n$ |
|------------------|---------|---------|-----|---------|
| $y_1$ | $p(x_1, y_1)$ | $p(x_2, y_1)$ | ... | $p(x_n, y_1)$ |
| $y_2$ | $p(x_1, y_2)$ | $p(x_2, y_2)$ | ... | $p(x_n, y_2)$ |
| ... | ... | ... | ... | ... |
| $y_m$ | $p(x_1, y_m)$ | $p(x_2, y_m)$ | ... | $p(x_n, y_m)$ |

**Свойство:** Сумма всех вероятностей в таблице равна 1.

---
# 🔍 Нахождение распределений составляющих
## Распределение составляющей $X$
Вероятность того, что $X$ примет значение $x_i$, равна сумме вероятностей "столбца $x_i$":

$$P(X = x_i) = \sum_{j=1}^m p(x_i, y_j)$$
## Распределение составляющей $Y$
Вероятность того, что $Y$ примет значение $y_j$, равна сумме вероятностей "строки $y_j$":

$$P(Y = y_j) = \sum_{i=1}^n p(x_i, y_j)$$

---
# 📈 Интегральная функция распределения
## Определение
> **Интегральной функцией распределения** двумерной случайной величины $(X, Y)$ называется функция $F(x, y)$, определяемая для любых пар чисел $x, y$ как вероятность того, что $X$ примет значение, меньшее $x$, и при этом $Y$ примет значение, меньшее $y$:

$$F(x, y) = P(X < x, Y < y)$$
## Свойства
1. $0 \leq F(x, y) \leq 1$
2. $F(x, y)$ - неубывающая функция по каждому аргументу
3. $F(+\infty, +\infty) = 1$
4. $F(-\infty, y) = 0$, $F(x, -\infty) = 0$

---
# 🔄 Условные распределения
## Условные вероятности
Если известно, что $Y = y_j$, то условные вероятности для $X$ определяются по формуле:

$$p(x_i|y_j) = \frac{p(x_i, y_j)}{p(y_j)}$$

Аналогично, если известно, что $X = x_i$, то условные вероятности для $Y$:

$$p(y_j|x_i) = \frac{p(x_i, y_j)}{p(x_i)}$$
## Условная функция распределения
Условная функция распределения $F(x|y)$ определяется как вероятность события $X < x$ при условии $Y = y$:

$$F(x|y) = P(X < x | Y = y)$$

---
# 🔗 Корреляционные характеристики
## Корреляционный момент
> **Корреляционным моментом** $\mu_{xy}$ случайных величин $X$ и $Y$ называется математическое ожидание произведения отклонений этих величин:

$$\mu_{xy} = M\{[X - M(X)][Y - M(Y)]\}$$

**Формула для вычисления:**
- Для дискретных величин:
  $$\mu_{xy} = \sum_{i=1}^n \sum_{j=1}^m [x_i - M(X)][y_j - M(Y)] \cdot p(x_i, y_j)$$
- Для непрерывных величин:
  $$\mu_{xy} = \int_{-\infty}^{\infty} \int_{-\infty}^{\infty} [x - M(X)][y - M(Y)] \cdot f(x, y) \, dx \, dy$$

**Упрощенная формула:**
$$\mu_{xy} = M(XY) - M(X) \cdot M(Y)$$
## Коэффициент корреляции
> **Коэффициентом корреляции** $r_{xy}$ случайных величин $X$ и $Y$ называется отношение корреляционного момента к произведению средних квадратичных отклонений:

$$r_{xy} = \frac{\mu_{xy}}{\sigma_x \cdot \sigma_y}$$

**Свойства коэффициента корреляции:**
1. $|r_{xy}| \leq 1$
2. Если $r_{xy} = 0$, то $X$ и $Y$ некоррелированы
3. Если $|r_{xy}| = 1$, то $X$ и $Y$ линейно зависимы

---
# 📉 Линейная регрессия
## Уравнение линейной регрессии
Если $X$ и $Y$ - зависимые случайные величины, то можно представить $Y$ как функцию $X$:

$$Y \approx g(X) = aX + b$$

**Уравнение линейной регрессии $Y$ на $X$:**

$$y - M(Y) = r_{xy} \cdot \frac{\sigma_y}{\sigma_x} (x - M(X))$$

**Коэффициенты регрессии:**
- Наклон: $a = r_{xy} \cdot \frac{\sigma_y}{\sigma_x}$
- Свободный член: $b = M(Y) - a \cdot M(X)$

---
# 📊 Пример расчета

**Дано распределение:**

| $Y \backslash X$ | 0.5 | 0.6 | 0.7 | 0.8 | 0.9 |
|------------------|-----|-----|-----|-----|-----|
| 0.5 | 0 | 0.02 | 0.06 | 0.08 | 0 |
| 0.6 | 0 | 0.04 | 0.1 | 0.09 | 0 |
| 0.7 | 0.02 | 0.2 | 0.03 | 0.01 | 0 |
| 0.8 | 0.2 | 0.1 | 0 | 0 | 0 |
| 0.9 | 0.04 | 0.01 | 0 | 0 | 0 |
## Расчет характеристик

**Для $X$:**
- $M(X) = 0.685$
- $D(X) = 0.01348$
- $\sigma_x = 0.116$

**Для $Y$:**
- $M(Y) = 0.629$
- $D(Y) = 0.01086$
- $\sigma_y = 0.104$

**Для совместного распределения:**
- $M(XY) = 0.421$
- $\mu_{xy} = M(XY) - M(X) \cdot M(Y) = -0.0086$
- $r_{xy} = \frac{\mu_{xy}}{\sigma_x \cdot \sigma_y} = -0.791$

## Уравнение регрессии
$$y - 0.629 = -0.791 \cdot \frac{0.104}{0.116} (x - 0.685)$$
$$y = -0.71x + 1.12$$
---
# 💡 Важные замечания

1. **Корреляционный момент** показывает тесноту линейной связи между случайными величинами.

2. **Коэффициент корреляции** нормирует корреляционный момент и позволяет сравнивать силу связи между различными парами случайных величин.

3. **Линейная регрессия** дает наилучшее линейное приближение зависимости одной случайной величины от другой в смысле метода наименьших квадратов.

4. **Независимые случайные величины** всегда некоррелированы ($r_{xy} = 0$), но обратное неверно - некоррелированные величины могут быть зависимы нелинейно.

---

## 📚 Полезные формулы на память

| Характеристика         | Формула                                                          |
| ---------------------- | ---------------------------------------------------------------- |
| Корреляционный момент  | $$\mu_{xy} = M(XY) - M(X) \cdot M(Y)$$                           |
| Коэффициент корреляции | $$r_{xy} = \frac{\mu_{xy}}{\sigma_x \cdot \sigma_y}$$            |
| Уравнение регрессии    | $$y - M(Y) = r_{xy} \cdot \frac{\sigma_y}{\sigma_x} (x - M(X))$$ |
| Условная вероятность   | $$p(x_iy_j) = \frac{p(x_i, y_j)}{p(y_j)}$$                       |


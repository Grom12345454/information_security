# 🌟 Дискретные случайные величины
## Определение случайной величины
> **Случайная величина** — это величина, которая в результате опыта может принимать то или иное значение с некоторой вероятностью.

**Типы случайных величин:**
- **Дискретные** — величины, число возможных значений которых конечно или счетно.
- **Непрерывные** — величины, возможные значения которых непрерывно заполняют некоторый интервал.

**Примеры:**
- Дискретные: число попаданий при трех выстрелах (0, 1, 2, 3), число вызовов, поступивших на телефонную станцию за сутки (0, 1, 2, 3, ...)
- Непрерывные: вес наугад взятого зерна пшеницы, скорость самолета в момент выхода на заданную высоту

**Обозначения:**
- Случайные величины обычно обозначают заглавными буквами: X, Y, Z
- Значения случайных величин обозначают строчными буквами: x₁, x₂, ..., xₙ

---
# 📋 Закон распределения дискретной случайной величины
## Определение
> **Закон распределения** — это соответствие, устанавливающее связь между значениями случайной величины и соответствующими вероятностями.

## Табличная форма
Для дискретной случайной величины X с возможными значениями x₁, x₂, ..., xₙ:

| X | x₁ | x₂ | ... | xₙ |
|---|----|----|-----|----|
| P | p₁ | p₂ | ... | pₙ |

Где P(X = xᵢ) = pᵢ и ∑pᵢ = 1 (так как значения случайной величины образуют полную группу несовместных событий)
## Пример
**Задача:** Вероятность того, что необходимая студенту книга свободна, равна 0.7. Составить закон распределения числа библиотек, которые посетит студент, если всего в городе их четыре.

**Решение:**
- p = 0.7 (свободна)
- q = 0.3 (занята)
- P₁ = 0.7
- P₂ = 0.3 · 0.7 = 0.21
- P₃ = 0.3 · 0.3 · 0.7 = 0.063
- P₄ = 0.3 · 0.3 · 0.3 = 0.027

| X | 1 | 2 | 3 | 4 |
|---|---|---|---|---|
| P | 0.7 | 0.21 | 0.063 | 0.027 |

---
# 📈 Числовые характеристики дискретных случайных величин
## Математическое ожидание
> **Математическое ожидание** — это средневзвешенное значение случайной величины, где весами являются вероятности значений.

**Формула:**
$$M(X) = x_1 \cdot p_1 + x_2 \cdot p_2 + \ldots + x_n \cdot p_n = \sum_{i=1}^n x_i \cdot p_i$$

**Свойства математического ожидания:**
1. $M(C) = C$ (математическое ожидание постоянной величины равно самой величине)
2. $M(M(X)) = M(X)$ (математическое ожидание от математического ожидания равно самому математическому ожиданию)
3. $M(C \cdot X) = C \cdot M(X)$ (постоянный множитель можно выносить за знак математического ожидания)
4. $M(X + Y) = M(X) + M(Y)$ (математическое ожидание суммы равно сумме математических ожиданий)
5. $M(X \cdot Y) = M(X) \cdot M(Y)$ (для независимых случайных величин)
## Пример
**Задача:** Найти математическое ожидание суммы очков при подбрасывании двух игральных костей.

**Решение:**
1. **Первый способ:**
   - X принимает значения от 2 до 12
   - $M(X) = 2 \cdot \frac{1}{36} + 3 \cdot \frac{1}{18} + 4 \cdot \frac{1}{12} + \ldots + 12 \cdot \frac{1}{36} = 7$

2. **Второй способ:**
   - Пусть Y — число очков на первой кости, Z — на второй
   - $M(Y) = M(Z) = \frac{1}{6}(1+2+3+4+5+6) = 3.5$
   - $M(X) = M(Y+Z) = M(Y) + M(Z) = 3.5 + 3.5 = 7$

---
## Мода
> **Мода** — это значение случайной величины, которому соответствует наибольшая вероятность.

**Пример:**
| X | 1 | 2 | 3 |
|---|---|---|---|
| P | 0.3 | 0.5 | 0.2 |

Мода = 2, так как вероятность P(X=2) = 0.5 — наибольшая.

---
## Медиана
> **Медиана** — это значение случайной величины, для которого выполняется условие: P(X < Me) = P(X > Me)

**Пример:**
| X | 1 | 2 | 5 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|
| P | 0.2 | 0.2 | 0.2 | 0.1 | 0.2 | 0.1 |

Медиана = 5, так как:
- P(X < 5) = 0.2 + 0.2 = 0.4
- P(X > 5) = 0.1 + 0.2 + 0.1 = 0.4

---
## Дисперсия
> **Дисперсия** — это математическое ожидание квадрата отклонения случайной величины от ее математического ожидания.

**Формулы:**
$$D(X) = M((X - M(X))^2)$$
$$D(X) = M(X^2) - (M(X))^2$$

**Свойства дисперсии:**
1. $D(C) = 0$ (дисперсия постоянной величины равна нулю)
2. $D(C \cdot X) = C^2 \cdot D(X)$ (постоянный множитель выносится за знак дисперсии в квадрате)
3. $D(X) = M(X^2) - (M(X))^2$ (альтернативная формула для вычисления)
4. $D(X + Y) = D(X) + D(Y)$ (для независимых случайных величин)

## Пример
**Задача:** Найти дисперсию дискретной случайной величины, заданной законом распределения:

| X | 0 | 1 | 2 |
|---|---|---|---|
| P | 0.3 | 0.2 | 0.5 |

**Решение:**
1. Сначала найдем математическое ожидание:
   $$M(X) = 0 \cdot 0.3 + 1 \cdot 0.2 + 2 \cdot 0.5 = 1.2$$

2. По первой формуле:
   $$D(X) = (0-1.2)^2 \cdot 0.3 + (1-1.2)^2 \cdot 0.2 + (2-1.2)^2 \cdot 0.5 = 0.76$$

3. По второй формуле:
   $$D(X) = 0^2 \cdot 0.3 + 1^2 \cdot 0.2 + 2^2 \cdot 0.5 - 1.2^2 = 2.2 - 1.44 = 0.76$$

---
## Среднее квадратичное отклонение
> **Среднее квадратичное отклонение** — это мера разброса значений случайной величины относительно математического ожидания.

**Формула:**
$$\sigma(X) = \sqrt{D(X)}$$

---
# 📊 Интегральная функция распределения
## Определение
> **Интегральная функция распределения** F(x) — это вероятность того, что случайная величина X примет значение меньше x.

**Формула:**
$$F(x) = P(X < x)$$
## Свойства интегральной функции
1. $0 \leq F(x) \leq 1$ (значения функции лежат в интервале [0,1])
2. $F(x)$ — неубывающая функция
3. Вероятность того, что случайная величина примет значение из интервала $(\alpha, \beta)$, равна разности значений интегральной функции на этом интервале: $P(\alpha \leq X \leq \beta) = F(\beta) - F(\alpha)$
4. Если все значения случайной величины принадлежат интервалу $(a, b)$, то:
   - $F(x) = 0$, при $x \leq a$
   - $F(x) = 1$, при $x > b$
5. $\lim_{x \to \infty} F(x) = 1$, $\lim_{x \to -\infty} F(x) = 0$
## Пример
Построить интегральную функцию распределения для случайной величины, заданной законом распределения:

| X | 1 | 2 | 3 | 4 |
|---|---|---|---|---|
| P | 0.2 | 0.3 | 0.4 | 0.1 |

**Решение:**
$$F(x) = 
\begin{cases}
0, & x \leq 1 \\
0.2, & 1 < x \leq 2 \\
0.5, & 2 < x \leq 3 \\
0.9, & 3 < x \leq 4 \\
1, & x > 4
\end{cases}$$
---
## 💡 Важные замечания

1. **Математическое ожидание** характеризует среднее значение случайной величины.

2. **Дисперсия** характеризует разброс значений случайной величины относительно математического ожидания.

3. **Интегральная функция распределения** позволяет определить вероятность попадания случайной величины в любой интервал.

4. **Мода** показывает наиболее вероятное значение случайной величины.

5. **Медиана** делит вероятностное распределение пополам.

---
# 📚 Полезные формулы на память

| Характеристика                  | Формула                                                |
| ------------------------------- | ------------------------------------------------------ |
| Математическое ожидание         | $$M(X) = \sum x_i \cdot p_i$$                          |
| Дисперсия (основная)            | $$D(X) = M((X - M(X))^2)$$                             |
| Дисперсия (альтернативная)      | $$D(X) = M(X^2) - (M(X))^2$$                           |
| Среднее квадратичное отклонение | $$\sigma(X) = \sqrt{D(X)}$$                            |
| Интегральная функция            | $$F(x) = P(X < x)$$                                    |
| Вероятность в интервале         | $$P(\alpha \leq X \leq \beta) = F(\beta) - F(\alpha)$$ |

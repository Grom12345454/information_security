# 🔷 Непрерывные случайные величины
## Определение непрерывной случайной величины
> **Непрерывная случайная величина** — это величина, возможные значения которой непрерывно заполняют некоторый интервал.

**Важное замечание:**  
Вероятность того, что непрерывная случайная величина примет точное значение, равна нулю:  
$$P(X = x) = 0$$

**Причина:**  
$$P(X = x) = \lim_{\beta \to \alpha} P(\alpha \leq X \leq \beta) = \lim_{\beta \to \alpha} [F(\beta) - F(\alpha)] = 0$$

**Следствие:**  
Вероятность того, что непрерывная случайная величина примет значение на отрезке $[a, b]$ или интервале $(a, b)$, одинакова:
$$P(a \leq X \leq b) = P(a < X < b) = P(a < X \leq b) = P(a \leq X < b)$$

---
# 📈 Дифференциальная функция распределения (плотность вероятности)
## Определение
Если интегральная функция распределения $F(x)$ непрерывна и дифференцируема, то её первая производная называется **дифференциальной функцией распределения** или **плотностью вероятности**:

$$f(x) = F'(x)$$
## Свойства плотности вероятности
1. **Не отрицательность**  
   $f(x) \geq 0$  
   *Поскольку $F(x)$ — неубывающая функция*

2. **Вероятность попадания в интервал**  
   Вероятность того, что случайная величина примет значение в интервале $[a, b]$, равна интегралу от плотности вероятности по этому интервалу:
   $$P(a \leq X \leq b) = \int_a^b f(x) \, dx$$

3. **Условие нормировки**  
   $$\int_{-\infty}^{\infty} f(x) \, dx = 1$$  
   *Поскольку это вероятность достоверного события $P(-\infty < X < \infty) = 1$*

4. **Связь с интегральной функцией**  
   Интегральную функцию можно найти как интеграл от плотности вероятности с переменным верхним пределом:
   $$F(x) = \int_{-\infty}^x f(t) \, dt$$

---
# 📊 Пример расчета плотности вероятности
**Задача:** Случайная величина $X$ распределена с плотностью:
$$f(x) = \begin{cases}
ax^2, & 0 < x < 3 \\
0, & x < 0, x > 3
\end{cases}$$

Найти:
1. Параметр $a$
2. Интегральную функцию $F(x)$
3. Вероятность того, что $X$ примет значение в интервале $[1, 2]$
4. Построить графики $f(x)$ и $F(x)$

**Решение:**
1. **Найдем параметр $a$:**  
   Используем условие нормировки:
   $$\int_{-\infty}^{\infty} f(x) \, dx = 1$$
   $$\int_0^3 ax^2 \, dx = a \cdot \left[\frac{x^3}{3}\right]_0^3 = a \cdot \frac{27}{3} = 9a = 1$$
   $$a = \frac{1}{9}$$
   
   Таким образом:
   $$f(x) = \begin{cases}
   \frac{1}{9}x^2, & 0 < x < 3 \\
   0, & x < 0, x > 3
   \end{cases}$$

2. **Найдем интегральную функцию $F(x)$:**  
   - При $x < 0$: $F(x) = \int_{-\infty}^x 0 \, dt = 0$
   - При $0 \leq x \leq 3$: 
     $$F(x) = \int_{-\infty}^0 0 \, dt + \int_0^x \frac{1}{9}t^2 \, dt = \frac{1}{9} \cdot \left[\frac{t^3}{3}\right]_0^x = \frac{x^3}{27}$$
   - При $x > 3$: 
     $$F(x) = \int_{-\infty}^0 0 \, dt + \int_0^3 \frac{1}{9}t^2 \, dt + \int_3^x 0 \, dt = \frac{27}{27} = 1$$
   
   Итак:
   $$F(x) = \begin{cases}
   0, & x < 0 \\
   \frac{x^3}{27}, & 0 \leq x \leq 3 \\
   1, & x > 3
   \end{cases}$$

3. **Вероятность попадания в интервал $[1, 2]$:**
   $$P(1 \leq X < 2) = \int_1^2 \frac{1}{9}x^2 \, dx = \frac{1}{9} \cdot \left[\frac{x^3}{3}\right]_1^2 = \frac{1}{27} \cdot (8 - 1) = \frac{7}{27}$$
---
# 📐 Числовые характеристики непрерывных случайных величин
## Математическое ожидание
> **Математическое ожидание** непрерывной случайной величины $X$ с плотностью вероятности $f(x)$:

$$M(X) = \int_{-\infty}^{\infty} x \cdot f(x) \, dx$$
## Дисперсия
> **Дисперсия** непрерывной случайной величины $X$:

$$D(X) = \int_{-\infty}^{\infty} (x - M(X))^2 \cdot f(x) \, dx$$

> **Альтернативная формула:**
$$D(X) = \int_{-\infty}^{\infty} x^2 \cdot f(x) \, dx - (M(X))^2$$
## Среднее квадратичное отклонение
> **Среднее квадратичное отклонение:**
$$\sigma(X) = \sqrt{D(X)}$$

---
# 📊 Пример вычисления характеристик

**Задача:** Найти математическое ожидание, дисперсию и среднее квадратичное отклонение для случайной величины, заданной плотностью вероятности из предыдущего примера:
$$f(x) = \begin{cases}
\frac{1}{9}x^2, & 0 \leq x \leq 3 \\
0, & x < 0, x > 3
\end{cases}$$

**Решение:**

1. **Математическое ожидание:**
   $$M(X) = \int_0^3 x \cdot \frac{1}{9}x^2 \, dx = \frac{1}{9} \int_0^3 x^3 \, dx = \frac{1}{9} \cdot \left[\frac{x^4}{4}\right]_0^3 = \frac{81}{36} = \frac{9}{4} = 2.25$$

2. **Дисперсия:**
   $$D(X) = \int_0^3 x^2 \cdot \frac{1}{9}x^2 \, dx - (M(X))^2 = \frac{1}{9} \int_0^3 x^4 \, dx - \left(\frac{9}{4}\right)^2$$
   $$= \frac{1}{9} \cdot \left[\frac{x^5}{5}\right]_0^3 - \frac{81}{16} = \frac{243}{45} - \frac{81}{16} = \frac{27}{5} - \frac{81}{16} = \frac{432 - 405}{80} = \frac{27}{80} = 0.3375$$

3. **Среднее квадратичное отклонение:**
   $$\sigma(X) = \sqrt{D(X)} = \sqrt{\frac{27}{80}} = \frac{3\sqrt{15}}{20} \approx 0.5809$$

---
# 📈 Пример с тригонометрической плотностью

**Задача:** Непрерывная случайная величина $X$ задана интегральной функцией распределения:
$$F(x) = \begin{cases}
0, & x < 0 \\
\frac{2}{\pi} \sin\frac{x}{2}, & 0 \leq x \leq \pi \\
1, & x > \pi
\end{cases}$$

Найти:
1. Плотность вероятности
2. Графики $f(x)$ и $F(x)$
3. Математическое ожидание и дисперсию
4. Вероятность попадания в интервал $[0, \frac{\pi}{4}]$

**Решение:**

1. **Плотность вероятности:**
   $$f(x) = F'(x) = \begin{cases}
   \frac{2}{\pi} \cdot \frac{1}{2} \cos\frac{x}{2} = \frac{1}{\pi} \cos\frac{x}{2}, & 0 \leq x \leq \pi \\
   0, & x < 0, x > \pi
   \end{cases}$$

2. **Вероятность попадания в $[0, \frac{\pi}{4}]$:**
   $$P(0 \leq X \leq \frac{\pi}{4}) = \int_0^{\frac{\pi}{4}} \frac{1}{\pi} \cos\frac{x}{2} \, dx = \frac{1}{\pi} \cdot 2 \left[\sin\frac{x}{2}\right]_0^{\frac{\pi}{4}} = \frac{2}{\pi} \sin\frac{\pi}{8} = \frac{2}{\pi} \cdot \frac{\sqrt{2-\sqrt{2}}}{2} = \frac{\sqrt{2-\sqrt{2}}}{\pi} \approx 0.2706$$

3. **Математическое ожидание:**
   $$M(X) = \int_0^\pi x \cdot \frac{1}{\pi} \cos\frac{x}{2} \, dx = \frac{1}{\pi} \int_0^\pi x \cos\frac{x}{2} \, dx$$
   Используя интегрирование по частям:
   $$= \frac{1}{\pi} \left[-2x \cos\frac{x}{2} + 4 \sin\frac{x}{2}\right]_0^\pi = \frac{1}{\pi} \left[-2\pi \cdot 0 + 4 \cdot 1 - 0\right] = \frac{4}{\pi} \approx 1.2732$$

4. **Дисперсия:**
   $$D(X) = \int_0^\pi x^2 \cdot \frac{1}{\pi} \cos\frac{x}{2} \, dx - (M(X))^2$$
   После вычислений:
   $$D(X) = \frac{\pi^2}{4} - 2 - \left(\frac{4}{\pi}\right)^2 \approx 0.465$$

---
# 💡 Важные замечания

1. **Непрерывные случайные величины** отличаются от дискретных тем, что их значения заполняют непрерывный интервал.

2. **Плотность вероятности** не является вероятностью, а представляет собой "плотность" вероятности в данной точке.

3. **Интегральная функция распределения** для непрерывной случайной величины является непрерывной и дифференцируемой функцией.

4. **Математическое ожидание** непрерывной случайной величины является центром распределения, аналогично дискретному случаю.

5. **Дисперсия** характеризует разброс значений относительно математического ожидания.

---
# 📚 Полезные формулы на память

| Характеристика                  | Формула                                                            |
| ------------------------------- | ------------------------------------------------------------------ |
| Плотность вероятности           | $$f(x) = F'(x)$$                                                   |
| Условие нормировки              | $$\int_{-\infty}^{\infty} f(x) \, dx = 1$$                         |
| Вероятность в интервале         | $$P(a \leq X \leq b) = \int_a^b f(x) \, dx$$                       |
| Математическое ожидание         | $$M(X) = \int_{-\infty}^{\infty} x \cdot f(x) \, dx$$              |
| Дисперсия (основная)            | $$D(X) = \int_{-\infty}^{\infty} (x - M(X))^2 \cdot f(x) \, dx$$   |
| Дисперсия (альтернативная)      | $$D(X) = \int_{-\infty}^{\infty} x^2 \cdot f(x) \, dx - (M(X))^2$$ |
| Среднее квадратичное отклонение | $$\sigma(X) = \sqrt{D(X)}$$                                        |
